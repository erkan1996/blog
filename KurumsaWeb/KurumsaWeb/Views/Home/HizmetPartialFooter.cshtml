@model IEnumerable<KurumsaWeb.Models.Model.Hizmet>
@{ Layout = null;}

<div class="col-lg-3">
	<div class="widget">
		<h5 class="widgetheading">Hizmetler</h5>
		<div class="flickr_badge">
			@foreach (var item in Model)
			{
				<img src="@item.ResimURL" width="75" height="30" />

			}

		</div>
		<div class="clear">
		</div>
	</div>
</div>